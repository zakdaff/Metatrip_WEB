<form method="post" action="{{ path('app_localisationvoyage_delete', {'idlocalisation': localisationvoyage.idlocalisation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ localisationvoyage.idlocalisation) }}">
    <button class="btn">Delete</button>
</form>
