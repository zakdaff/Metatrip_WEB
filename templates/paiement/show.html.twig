{% extends 'base.html.twig' %}

{% block title %}Paiement{% endblock %}

{% block body %}
    <h1>Paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>RefPaiement</th>
                <td>{{ paiement.refPaiement }}</td>
            </tr>
            <tr>
                <th>DatePaiement</th>
                <td>{{ paiement.datePaiement ? paiement.datePaiement|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_paiement_index') }}">back to list</a>

    <a href="{{ path('app_paiement_edit', {'refPaiement': paiement.refPaiement}) }}">edit</a>

    {{ include('paiement/_delete_form.html.twig') }}
{% endblock %}
