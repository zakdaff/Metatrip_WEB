<form method="post" action="{{ path('app_reservation_voyage_delete', {'idrv': reservation_voyage.idrv}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_voyage.idrv) }}">
    <button class="btn">Delete</button>
</form>
