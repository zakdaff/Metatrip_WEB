<form method="post" action="{{ path('app_voyage_delete', {'idv': voyage.idv}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voyage.idv) }}">
    <button class="btn">Delete</button>
</form>
