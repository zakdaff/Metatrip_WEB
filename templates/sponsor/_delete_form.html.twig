<form method="post" action="{{ path('app_sponsor_delete', {'ids': sponsor.ids}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sponsor.ids) }}">
    <button class="btn">Delete</button>
</form>
