<form method="post" action="{{ path('app_voyage_virtuel_delete', {'idvv': voyage_virtuel.idvv}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voyage_virtuel.idvv) }}">
    <button class="btn">Delete</button>
</form>
