{% extends 'base.html.twig' %}

{% block title %}Chambre{% endblock %}

{% block body %}
    <h1>Chambre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idc</th>
                <td>{{ chambre.idc }}</td>
            </tr>
            <tr>
                <th>Numc</th>
                <td>{{ chambre.numc }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ chambre.image }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ chambre.type }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ chambre.etat }}</td>
            </tr>
            <tr>
                <th>Prixc</th>
                <td>{{ chambre.prixc }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chambre_index') }}">back to list</a>

    <a href="{{ path('app_chambre_edit', {'idc': chambre.idc}) }}">edit</a>

    {{ include('chambre/_delete_form.html.twig') }}
{% endblock %}
