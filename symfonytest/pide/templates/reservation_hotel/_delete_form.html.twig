<form method="post" action="{{ path('app_reservation_hotel_delete', {'idrh': reservation_hotel.idrh}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_hotel.idrh) }}">
    <button class="btn">Delete</button>
</form>
